// This program uses for loop to count numbers from 1 to 10
// For loop structure: "for(initialization;condition;updation)"

#include <stdio.h>

int main()
{
    int a;
    for (a = 1; a <= 10; a++)
    {
        printf("\n%d", a);
    }
}